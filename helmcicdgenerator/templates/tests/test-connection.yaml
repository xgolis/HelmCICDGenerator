apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmcicdgenerator.fullname" . }}-test-connection"
  labels:
    {{- include "helmcicdgenerator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmcicdgenerator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
